devpulse/
├── 📄 .gitignore                <-- Criado na raiz para gerenciar o monorepo.
├── 📖 README.md                 <-- Documentação principal do projeto.
│
├── 🐘 api-devpulse/             (BACKEND - Laravel)
│   ├── 📄 .env                  <-- Configuração do Banco de Dados MySQL.
│   ├── 📂 app/Models/
│   │   └── 📄 Project.php       <-- Onde definimos a tabela de projetos.
│   ├── 📂 app/Http/Controllers/
│   │   └── 📄 ProjectController.php <-- Lógica para buscar os dados no banco.
│   ├── 📂 database/
│   │   ├── 📂 migrations/
│   │   │   └── 📄 create_projects_table.php <-- Estrutura da tabela no MySQL.
│   │   └── 📂 seeders/
│   │       └── 📄 ProjectSeeder.php <-- Onde você "plantou" seus projetos reais.
│   └── 📂 routes/
│       └── 📄 api.php           <-- Onde criamos a rota /api/projects.
│
└── 🌐 web-devpulse/             (FRONTEND - React)
    ├── 📂 src/
    │   ├── 📄 App.jsx           <-- O coração do site (Axios + Cards Bootstrap).
    │   ├── 📄 main.jsx          <-- Onde o React é inicializado.
    │   └── 📄 index.css         <-- Onde colocamos o efeito de "Hover" nos cards.
    └── 📄 package.json          <-- Onde instalamos o Axios e o Lucide-React.